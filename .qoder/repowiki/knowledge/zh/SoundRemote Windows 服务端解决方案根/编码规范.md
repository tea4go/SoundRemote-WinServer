- PowerShell 入口脚本通过 `[switch]` 参数实现多阶段子命令模式（-Check / -InstallDeps / -Build / -Run / -Test），每个阶段独立分支处理，无嵌套调用。
- 工具链查找遵循「优先 PATH → VS 内置 → 用户目录」降级策略（Find-NuGet / Find-Vcpkg），失败时给出带修复提示的 Write-CheckItem 输出。
- vcpkg 依赖安装前检查关键头文件是否存在（boost\asio.hpp、opus\opus.h、SimpleIni.h），存在则跳过以避免触发 cmake 等工具链下载。