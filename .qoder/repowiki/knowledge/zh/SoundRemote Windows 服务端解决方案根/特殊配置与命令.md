首次构建需先执行 `.un_win.ps1 -InstallDeps` 安装 NuGet/gmock 及 vcpkg 依赖，再 `.un_win.ps1 -Build -Configuration Release -Platform x64`；也可直接 `.un_win.ps1 -Run` 一键启动已构建产物。CI 通过 `VCPKG_BINARY_SOURCES` 环境变量配合 TAServers/vcpkg-cache 缓存 vcpkg 预编译二进制。