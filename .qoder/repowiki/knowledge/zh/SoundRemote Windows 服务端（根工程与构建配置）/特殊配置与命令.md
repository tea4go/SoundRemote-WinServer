本地需安装 Visual Studio 2022 并将 vcpkg 集成到 MSBuild；常用命令：
- `.uild_win.ps1 -Build -Configuration Release -Platform x64` 构建
- `.uild_win.ps1 -Run` 停止旧进程并启动最新构建
- `.uild_win.ps1 -Test` 运行 Tests.exe
- CI 触发条件为 push/pull_request 至 main 分支。