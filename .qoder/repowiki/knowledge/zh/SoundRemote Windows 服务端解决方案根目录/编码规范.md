- 构建脚本以 PowerShell 函数封装外部工具发现（如 Initialize-VsEnvironment、Find-NuGet、Find-Vcpkg），缺失时给出警告而非直接失败，保证在无完整环境的机器上仍可尝试构建。
- 所有平台/配置变量（Configuration、Platform、Output）均以可选参数形式暴露并提供默认值，保持命令行向后兼容。
- 构建前自动调用 Get-Process 终止同名旧进程，避免可执行文件被锁导致 MSBuild 失败。
- vcpkg 依赖安装前先检查关键头文件是否存在，已就绪则跳过安装以避免触发 cmake 等重型工具链下载。